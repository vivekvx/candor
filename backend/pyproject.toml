[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "candor-backend"
version = "0.1.0"
description = "Candor multi-agent career intelligence backend"
requires-python = ">=3.11"
